apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-avi.fullname" . }}
  labels:
    {{- include "my-app-avi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-app-avi.selectorLabels" . | nindent 4 }}
